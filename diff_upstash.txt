diff --git a/scripts/auto-login.js b/scripts/auto-login.js
index 938b844..3ff42e9 100644
--- a/scripts/auto-login.js
+++ b/scripts/auto-login.js
@@ -90,7 +90,15 @@ async function storeTokenInRedis(accessToken) {
   for (let attempt = 1; attempt <= MAX_RETRIES; attempt++) {
     try {
       const { default: Redis } = await import('ioredis');
-      const redis = new Redis(REDIS_URL, { keyPrefix: 'alpha8:', lazyConnect: true });
+      const url = (REDIS_URL.includes('upstash.io') && REDIS_URL.startsWith('redis://'))
+        ? REDIS_URL.replace('redis://', 'rediss://')
+        : REDIS_URL;
+
+      const redis = new Redis(url, {
+        keyPrefix: 'alpha8:',
+        lazyConnect: true,
+        ...(url.startsWith('rediss://') && { tls: { rejectUnauthorized: false } })
+      });
 
       try {
         await redis.connect();
@@ -129,7 +137,15 @@ async function storeTokenInRedis(accessToken) {
 async function engageKillSwitch(reason) {
   try {
     const { default: Redis } = await import('ioredis');
-    const redis = new Redis(REDIS_URL, { keyPrefix: 'alpha8:', lazyConnect: true });
+    const url = (REDIS_URL.includes('upstash.io') && REDIS_URL.startsWith('redis://'))
+      ? REDIS_URL.replace('redis://', 'rediss://')
+      : REDIS_URL;
+
+    const redis = new Redis(url, {
+      keyPrefix: 'alpha8:',
+      lazyConnect: true,
+      ...(url.startsWith('rediss://') && { tls: { rejectUnauthorized: false } })
+    });
 
     await redis.connect();
     const killState = JSON.stringify({
diff --git a/scripts/setup-db.js b/scripts/setup-db.js
index cb9797e..e550f1f 100644
--- a/scripts/setup-db.js
+++ b/scripts/setup-db.js
@@ -17,17 +17,17 @@ import { fileURLToPath } from 'url';
 dotenvConfig({ path: path.resolve(path.dirname(fileURLToPath(import.meta.url)), '../.env') });
 
 const DATABASE_URL =
-    process.env.DATABASE_URL || 'postgresql://alpha8:alpha8_dev@localhost:5432/alpha8';
+  process.env.DATABASE_URL || 'postgresql://alpha8:alpha8_dev@localhost:5432/alpha8';
 
 const { Client } = pg;
 
 const MIGRATIONS = [
 
-    // ΓöÇΓöÇΓöÇ 1. TRADES ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
-    // Writers : execution-engine.js, square-off-job.js
-    // Readers : backend-api.js
-    // Key constraint: order_id UNIQUE NOT NULL ΓÇö app uses ON CONFLICT (order_id) DO NOTHING
-    `CREATE TABLE IF NOT EXISTS trades (
+  // ΓöÇΓöÇΓöÇ 1. TRADES ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
+  // Writers : execution-engine.js, square-off-job.js
+  // Readers : backend-api.js
+  // Key constraint: order_id UNIQUE NOT NULL ΓÇö app uses ON CONFLICT (order_id) DO NOTHING
+  `CREATE TABLE IF NOT EXISTS trades (
     id         SERIAL PRIMARY KEY,
     order_id   VARCHAR(100) UNIQUE NOT NULL,
     broker_id  VARCHAR(100),
@@ -44,16 +44,16 @@ const MIGRATIONS = [
     filled_at  TIMESTAMPTZ
   );`,
 
-    `CREATE INDEX IF NOT EXISTS idx_trades_symbol     ON trades(symbol);`,
-    `CREATE INDEX IF NOT EXISTS idx_trades_strategy   ON trades(strategy);`,
-    `CREATE INDEX IF NOT EXISTS idx_trades_created_at ON trades(created_at);`,
-    `CREATE INDEX IF NOT EXISTS idx_trades_status     ON trades(status);`,
+  `CREATE INDEX IF NOT EXISTS idx_trades_symbol     ON trades(symbol);`,
+  `CREATE INDEX IF NOT EXISTS idx_trades_strategy   ON trades(strategy);`,
+  `CREATE INDEX IF NOT EXISTS idx_trades_created_at ON trades(created_at);`,
+  `CREATE INDEX IF NOT EXISTS idx_trades_status     ON trades(status);`,
 
-    // ΓöÇΓöÇΓöÇ 2. POSITIONS ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
-    // Writers : nothing in current codebase (populated externally / future)
-    // Readers : backend-api.js ΓÇö reads avg_price, current_price, stop_loss, product, strategy
-    // NO UNIQUE on symbol ΓÇö multiple position rows per symbol must be allowed
-    `CREATE TABLE IF NOT EXISTS positions (
+  // ΓöÇΓöÇΓöÇ 2. POSITIONS ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
+  // Writers : nothing in current codebase (populated externally / future)
+  // Readers : backend-api.js ΓÇö reads avg_price, current_price, stop_loss, product, strategy
+  // NO UNIQUE on symbol ΓÇö multiple position rows per symbol must be allowed
+  `CREATE TABLE IF NOT EXISTS positions (
     id            SERIAL PRIMARY KEY,
     symbol        VARCHAR(20)  NOT NULL,
     side          VARCHAR(4)   NOT NULL CHECK (side IN ('BUY', 'SELL')),
@@ -68,14 +68,14 @@ const MIGRATIONS = [
     updated_at    TIMESTAMPTZ  DEFAULT NOW()
   );`,
 
-    `CREATE INDEX IF NOT EXISTS idx_positions_symbol    ON positions(symbol);`,
-    `CREATE INDEX IF NOT EXISTS idx_positions_opened_at ON positions(opened_at);`,
+  `CREATE INDEX IF NOT EXISTS idx_positions_symbol    ON positions(symbol);`,
+  `CREATE INDEX IF NOT EXISTS idx_positions_opened_at ON positions(opened_at);`,
 
-    // ΓöÇΓöÇΓöÇ 3. SIGNALS ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
-    // Writers : execution-engine.js ΓÇö INSERT (symbol, strategy, signal, confidence, acted_on, reason, created_at)
-    //           execution-engine.js ΓÇö UPDATE signals SET acted_on = true
-    // Readers : backend-api.js ΓÇö reads strategy, symbol, signal, confidence, acted_on, created_at
-    `CREATE TABLE IF NOT EXISTS signals (
+  // ΓöÇΓöÇΓöÇ 3. SIGNALS ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
+  // Writers : execution-engine.js ΓÇö INSERT (symbol, strategy, signal, confidence, acted_on, reason, created_at)
+  //           execution-engine.js ΓÇö UPDATE signals SET acted_on = true
+  // Readers : backend-api.js ΓÇö reads strategy, symbol, signal, confidence, acted_on, created_at
+  `CREATE TABLE IF NOT EXISTS signals (
     id         SERIAL PRIMARY KEY,
     symbol     VARCHAR(20) NOT NULL,
     strategy   VARCHAR(50) NOT NULL,
@@ -87,16 +87,16 @@ const MIGRATIONS = [
     created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
   );`,
 
-    `CREATE INDEX IF NOT EXISTS idx_signals_symbol     ON signals(symbol);`,
-    `CREATE INDEX IF NOT EXISTS idx_signals_strategy   ON signals(strategy);`,
-    `CREATE INDEX IF NOT EXISTS idx_signals_created_at ON signals(created_at);`,
+  `CREATE INDEX IF NOT EXISTS idx_signals_symbol     ON signals(symbol);`,
+  `CREATE INDEX IF NOT EXISTS idx_signals_strategy   ON signals(strategy);`,
+  `CREATE INDEX IF NOT EXISTS idx_signals_created_at ON signals(created_at);`,
 
-    // ΓöÇΓöÇΓöÇ 4. DAILY SUMMARY ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
-    // Writers : nothing in current codebase (populated externally / future)
-    // Readers : backend-api.js ΓÇö reads pnl, pnl_pct, trade_count, win_count, loss_count,
-    //                             filled, rejected, best_trade, worst_trade
-    // best_trade / worst_trade are NUMERIC ΓÇö backend does parseFloat(dbSummary.best_trade)
-    `CREATE TABLE IF NOT EXISTS daily_summary (
+  // ΓöÇΓöÇΓöÇ 4. DAILY SUMMARY ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
+  // Writers : nothing in current codebase (populated externally / future)
+  // Readers : backend-api.js ΓÇö reads pnl, pnl_pct, trade_count, win_count, loss_count,
+  //                             filled, rejected, best_trade, worst_trade
+  // best_trade / worst_trade are NUMERIC ΓÇö backend does parseFloat(dbSummary.best_trade)
+  `CREATE TABLE IF NOT EXISTS daily_summary (
     id          SERIAL PRIMARY KEY,
     trade_date  DATE         NOT NULL UNIQUE,
     pnl         NUMERIC(12,2) DEFAULT 0,
@@ -111,25 +111,25 @@ const MIGRATIONS = [
     created_at  TIMESTAMPTZ  DEFAULT NOW()
   );`,
 
-    `CREATE INDEX IF NOT EXISTS idx_daily_summary_date ON daily_summary(trade_date);`,
+  `CREATE INDEX IF NOT EXISTS idx_daily_summary_date ON daily_summary(trade_date);`,
 
-    // ΓöÇΓöÇΓöÇ 5. SETTINGS ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
-    // Writers : backend-api.js        ΓÇö stores watchlist as JSON.stringify(array)
-    //           symbol-scout.js       ΓÇö stores dynamic_watchlist as JSON.stringify(array)
-    // Readers : backend-api.js, symbol-scout.js ΓÇö both do JSON.parse(result.rows[0].value)
-    // value MUST be TEXT ΓÇö app serialises/deserialises manually with JSON.stringify/parse.
-    // JSONB would auto-deserialise on read, breaking JSON.parse() calls.
-    `CREATE TABLE IF NOT EXISTS settings (
+  // ΓöÇΓöÇΓöÇ 5. SETTINGS ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
+  // Writers : backend-api.js        ΓÇö stores watchlist as JSON.stringify(array)
+  //           symbol-scout.js       ΓÇö stores dynamic_watchlist as JSON.stringify(array)
+  // Readers : backend-api.js, symbol-scout.js ΓÇö both do JSON.parse(result.rows[0].value)
+  // value MUST be TEXT ΓÇö app serialises/deserialises manually with JSON.stringify/parse.
+  // JSONB would auto-deserialise on read, breaking JSON.parse() calls.
+  `CREATE TABLE IF NOT EXISTS settings (
     key        VARCHAR(100) PRIMARY KEY,
     value      TEXT         NOT NULL,
     updated_at TIMESTAMPTZ  DEFAULT NOW()
   );`,
 
-    // ΓöÇΓöÇΓöÇ 6. SIGNAL OUTCOMES ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
-    // Writers : adaptive-weights.js ΓÇö INSERT (strategy, signal, symbol, outcome, pnl, recorded_at)
-    // Readers : adaptive-weights.js ΓÇö SELECT per strategy for weight calculation
-    //           symbol-scout.js     ΓÇö SELECT per symbol for track record scoring + consecutive losses
-    `CREATE TABLE IF NOT EXISTS signal_outcomes (
+  // ΓöÇΓöÇΓöÇ 6. SIGNAL OUTCOMES ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
+  // Writers : adaptive-weights.js ΓÇö INSERT (strategy, signal, symbol, outcome, pnl, recorded_at)
+  // Readers : adaptive-weights.js ΓÇö SELECT per strategy for weight calculation
+  //           symbol-scout.js     ΓÇö SELECT per symbol for track record scoring + consecutive losses
+  `CREATE TABLE IF NOT EXISTS signal_outcomes (
     id          SERIAL PRIMARY KEY,
     strategy    VARCHAR(50)  NOT NULL,
     signal      VARCHAR(10)  NOT NULL,
@@ -139,13 +139,13 @@ const MIGRATIONS = [
     recorded_at TIMESTAMPTZ  NOT NULL DEFAULT NOW()
   );`,
 
-    `CREATE INDEX IF NOT EXISTS idx_signal_outcomes_strategy ON signal_outcomes(strategy, recorded_at);`,
-    `CREATE INDEX IF NOT EXISTS idx_signal_outcomes_symbol   ON signal_outcomes(symbol, recorded_at);`,
+  `CREATE INDEX IF NOT EXISTS idx_signal_outcomes_strategy ON signal_outcomes(strategy, recorded_at);`,
+  `CREATE INDEX IF NOT EXISTS idx_signal_outcomes_symbol   ON signal_outcomes(symbol, recorded_at);`,
 
-    // ΓöÇΓöÇΓöÇ 7. SYMBOL SCORES ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
-    // Writers : symbol-scout.js ΓÇö INSERT nightly score snapshot per symbol
-    // Readers : symbol-scout.js ΓÇö SELECT latest scan for dashboard display
-    `CREATE TABLE IF NOT EXISTS symbol_scores (
+  // ΓöÇΓöÇΓöÇ 7. SYMBOL SCORES ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
+  // Writers : symbol-scout.js ΓÇö INSERT nightly score snapshot per symbol
+  // Readers : symbol-scout.js ΓÇö SELECT latest scan for dashboard display
+  `CREATE TABLE IF NOT EXISTS symbol_scores (
     id         SERIAL PRIMARY KEY,
     symbol     VARCHAR(20) NOT NULL,
     score      INTEGER     NOT NULL CHECK (score BETWEEN 0 AND 100),
@@ -153,13 +153,13 @@ const MIGRATIONS = [
     scanned_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
   );`,
 
-    `CREATE INDEX IF NOT EXISTS idx_symbol_scores_symbol     ON symbol_scores(symbol, scanned_at DESC);`,
-    `CREATE INDEX IF NOT EXISTS idx_symbol_scores_scanned_at ON symbol_scores(scanned_at DESC);`,
+  `CREATE INDEX IF NOT EXISTS idx_symbol_scores_symbol     ON symbol_scores(symbol, scanned_at DESC);`,
+  `CREATE INDEX IF NOT EXISTS idx_symbol_scores_scanned_at ON symbol_scores(scanned_at DESC);`,
 
-    // ΓöÇΓöÇΓöÇ 8. WATCHLIST LOG ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
-    // Writers : symbol-scout.js ΓÇö INSERT on every add/remove
-    // Readers : dashboard (audit trail of scout decisions)
-    `CREATE TABLE IF NOT EXISTS watchlist_log (
+  // ΓöÇΓöÇΓöÇ 8. WATCHLIST LOG ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
+  // Writers : symbol-scout.js ΓÇö INSERT on every add/remove
+  // Readers : dashboard (audit trail of scout decisions)
+  `CREATE TABLE IF NOT EXISTS watchlist_log (
     id        SERIAL PRIMARY KEY,
     symbol    VARCHAR(20) NOT NULL,
     action    VARCHAR(10) NOT NULL CHECK (action IN ('ADDED', 'REMOVED', 'PINNED')),
@@ -168,75 +168,75 @@ const MIGRATIONS = [
     logged_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
   );`,
 
-    `CREATE INDEX IF NOT EXISTS idx_watchlist_log_symbol    ON watchlist_log(symbol, logged_at DESC);`,
-    `CREATE INDEX IF NOT EXISTS idx_watchlist_log_logged_at ON watchlist_log(logged_at DESC);`,
+  `CREATE INDEX IF NOT EXISTS idx_watchlist_log_symbol    ON watchlist_log(symbol, logged_at DESC);`,
+  `CREATE INDEX IF NOT EXISTS idx_watchlist_log_logged_at ON watchlist_log(logged_at DESC);`,
 ];
 
 // ΓöÇΓöÇΓöÇ Seed data ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
 // Inserted once. ON CONFLICT DO NOTHING means re-runs are safe.
 const SEEDS = [
-    // Default pinned watchlist ΓÇö overridden by WATCHLIST in .env at runtime,
-    // but stored here so the settings table is never empty on first boot.
-    `INSERT INTO settings (key, value, updated_at)
+  // Default pinned watchlist ΓÇö overridden by WATCHLIST in .env at runtime,
+  // but stored here so the settings table is never empty on first boot.
+  `INSERT INTO settings (key, value, updated_at)
    VALUES ('watchlist', '["RELIANCE","INFY","TCS","HDFCBANK","SBIN"]', NOW())
    ON CONFLICT (key) DO NOTHING;`,
 
-    // Empty dynamic watchlist ΓÇö scout fills this on its first nightly run.
-    `INSERT INTO settings (key, value, updated_at)
+  // Empty dynamic watchlist ΓÇö scout fills this on its first nightly run.
+  `INSERT INTO settings (key, value, updated_at)
    VALUES ('dynamic_watchlist', '[]', NOW())
    ON CONFLICT (key) DO NOTHING;`,
 ];
 
 // ΓöÇΓöÇΓöÇ Runner ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
 async function setup() {
-    console.log('≡ƒùä∩╕Å  Alpha8 Database Setup');
-    console.log('ΓöÇ'.repeat(55));
-    console.log(`   Connecting to: ${DATABASE_URL.replace(/:\/\/.*@/, '://<credentials>@')}`);
-    console.log('ΓöÇ'.repeat(55));
+  console.log('≡ƒùä∩╕Å  Alpha8 Database Setup');
+  console.log('ΓöÇ'.repeat(55));
+  console.log(`   Connecting to: ${DATABASE_URL.replace(/:\/\/.*@/, '://<credentials>@')}`);
+  console.log('ΓöÇ'.repeat(55));
+
+  const client = new Client({ connectionString: DATABASE_URL });
+
+  try {
+    await client.connect();
+    console.log('Γ£à Connected to PostgreSQL\n');
+
+    await client.query('BEGIN');
+
+    for (const sql of MIGRATIONS) {
+      const isTable = sql.trimStart().startsWith('CREATE TABLE');
+      const isIndex = sql.trimStart().startsWith('CREATE INDEX');
+
+      if (isTable) {
+        const name = sql.match(/CREATE TABLE\s+(?:IF NOT EXISTS\s+)?(\w+)/i)?.[1] ?? 'unknown';
+        process.stdout.write(`  Γû╕ table: ${name.padEnd(22)} `);
+      } else if (isIndex) {
+        const name = sql.match(/CREATE INDEX\s+(?:IF NOT EXISTS\s+)?(\w+)/i)?.[1] ?? 'unknown';
+        process.stdout.write(`    index: ${name.padEnd(22)} `);
+      }
 
-    const client = new Client({ connectionString: DATABASE_URL });
-
-    try {
-        await client.connect();
-        console.log('Γ£à Connected to PostgreSQL\n');
-
-        await client.query('BEGIN');
-
-        for (const sql of MIGRATIONS) {
-            const isTable = sql.trimStart().startsWith('CREATE TABLE');
-            const isIndex = sql.trimStart().startsWith('CREATE INDEX');
-
-            if (isTable) {
-                const name = sql.match(/CREATE TABLE\s+(?:IF NOT EXISTS\s+)?(\w+)/i)?.[1] ?? 'unknown';
-                process.stdout.write(`  Γû╕ table: ${name.padEnd(22)} `);
-            } else if (isIndex) {
-                const name = sql.match(/CREATE INDEX\s+(?:IF NOT EXISTS\s+)?(\w+)/i)?.[1] ?? 'unknown';
-                process.stdout.write(`    index: ${name.padEnd(22)} `);
-            }
-
-            await client.query(sql);
-            console.log('Γ£à');
-        }
-
-        console.log('\n  Seeding defaults...');
-        for (const sql of SEEDS) {
-            await client.query(sql);
-        }
-        console.log('  Γ£à Default settings seeded\n');
-
-        await client.query('COMMIT');
-
-        console.log('ΓöÇ'.repeat(55));
-        console.log('≡ƒÄë Database ready ΓÇö all tables and indexes created.');
-        console.log('ΓöÇ'.repeat(55));
-    } catch (err) {
-        await client.query('ROLLBACK');
-        console.error('\nΓ¥î Setup failed:', err.message);
-        console.error('   All changes have been rolled back.');
-        process.exit(1);
-    } finally {
-        await client.end();
+      await client.query(sql);
+      console.log('Γ£à');
     }
+
+    console.log('\n  Seeding defaults...');
+    for (const sql of SEEDS) {
+      await client.query(sql);
+    }
+    console.log('  Γ£à Default settings seeded\n');
+
+    await client.query('COMMIT');
+
+    console.log('ΓöÇ'.repeat(55));
+    console.log('≡ƒÄë Database ready ΓÇö all tables and indexes created.');
+    console.log('ΓöÇ'.repeat(55));
+  } catch (err) {
+    await client.query('ROLLBACK');
+    console.error('\nΓ¥î Setup failed:', err.message);
+    console.error('   All changes have been rolled back.');
+    process.exit(1);
+  } finally {
+    await client.end();
+  }
 }
 
 setup();
\ No newline at end of file
diff --git a/src/lib/redis.js b/src/lib/redis.js
index 6d61bd8..c0c6528 100644
--- a/src/lib/redis.js
+++ b/src/lib/redis.js
@@ -13,7 +13,12 @@ let redisClient = null;
  * @returns {Redis} The Redis client instance
  */
 export function initRedis(url) {
-  redisClient = new Redis(url, {
+  // Auto-upgrade Upstash connections to use TLS (rediss://)
+  if (url.includes('upstash.io') && url.startsWith('redis://')) {
+    url = url.replace('redis://', 'rediss://');
+  }
+
+  const options = {
     keyPrefix: 'alpha8:',
     maxRetriesPerRequest: 3,
     retryStrategy(times) {
@@ -22,7 +27,14 @@ export function initRedis(url) {
       return delay;
     },
     lazyConnect: true,
-  });
+  };
+
+  // Inject TLS config if using a secure URL
+  if (url.startsWith('rediss://')) {
+    options.tls = { rejectUnauthorized: false };
+  }
+
+  redisClient = new Redis(url, options);
 
   redisClient.on('connect', () => {
     log.info('Redis connected');
